import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'windows-run-command-quiz',
  webDir: 'dist'
};

export default config;
